version: "3.7"
services:
  graph-bot:
    image: "graph-bot"
    container_name: "graph-bot"
    volumes:
      - "./config:/config"
      - "/var/run/docker.sock:/var/run/docker.sock"
    environment:
      DATA_PATH: '/config'
    restart: unless-stopped
